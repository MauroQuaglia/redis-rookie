# https://hub.docker.com/r/redis/redisinsight
# > docker compose up
# http://localhost:5540/

services:
  redisinsight:
    image: "redis/redisinsight:latest"
    volumes:
      - "redisinsight:/data"
    ports:
      - "5540:5540"
volumes:
  redisinsight: